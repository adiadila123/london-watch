[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r server/requirements.txt"]

[start]
cmd = "gunicorn --chdir server --workers 1 --threads 4 app:app --bind 0.0.0.0:$PORT"
